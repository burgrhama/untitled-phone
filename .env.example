# Backend Environment Variables

# For Render, add these as environment variables in the dashboard
# Do NOT commit actual secrets to GitHub

# JWT Secret Key (change this to a random string)
SECRET_KEY=your-secure-random-string-here

# Server Port (Render will override this)
PORT=3000

# Database
# SQLite is used by default (users.db)
# For production, use PostgreSQL
